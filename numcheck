#include <iostream>
using namespace std;

int main() 
{
	int no;
	cin>>no;
	if(no>0)
	{
	cout<<"its positive \t";
	}
	else if(no<0)
	{
	cout<<"its negative\t";
	}
	else 
	{
	cout<<"its zero";
	}
	return 0;
}
